name: flutter_uniapp
description: A new Flutter plugin.
version: 0.0.1
author:
homepage: https://github.com/yldebug/flutter_uniapp

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: com.example.flutter_uniapp
        pluginClass: FlutterUniappPlugin
      ios:
        pluginClass: FlutterUniappPlugin


